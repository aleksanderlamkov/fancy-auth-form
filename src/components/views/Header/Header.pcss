@import "../../../styles/mixins.pcss";

.header {
  &__inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    column-gap: 20px;
    padding-block: 12px;
    box-shadow: var(--box-shadow-3);

    @media (--mobile) {
      column-gap: 16px;
      padding-block: 8px;
    }
  }

  &__actions {
    display: flex;
    align-items: center;
    column-gap: 8px;
  }

  &__button {
    && {
      width: auto;
    }
  }
}
