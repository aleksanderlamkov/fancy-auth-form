@import "../../../styles/mixins.pcss";

.footer {
  &__inner {
    display: flex;
    justify-content: flex-end;
    padding-block: 10px;
    box-shadow: var(--box-shadow-4);
  }

  &__copyright-link {
    font-size: 12px;
    text-decoration: none;
  }
}
